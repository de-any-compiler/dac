//! `dac-binfmt` — binary format parsing (ELF, PE, Mach-O) for dac.
//!
//! Part of the dac workspace. See `ARCHITECTURE.md` in the workspace root.
//!
//! Status: stub. ELF lands with `B1.1`, PE with `B1.2`.

#![forbid(unsafe_code)]
